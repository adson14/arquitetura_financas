version: '3'

services:
  app:
    build: .
    ports:
      - '3001:3000'
    networks:
      - arquitetura-network
    entrypoint: ./.docker/entrypoint.sh
    volumes:
      - '.:/home/node/app'
networks:
  arquitetura-network:
    driver: bridge
